# Fase 0 — versiones mínimas, no fijadas todavía.
# Una vez que cada proyecto pase por "Adaptación GitHub" y se pruebe localmente,
# fijar la versión exacta (==) con la que el modelo serializado fue entrenado,
# para evitar que joblib.load()/keras.load_model() falle por incompatibilidad.
#
# NOTA: tensorflow se agrega de vuelta al llegar a la adaptación de
# 02_Redes_Neuronales — Streamlit Cloud no tenía wheel disponible para la
# versión de Python activa en este momento (ver error de deploy, 24-06-2026).

streamlit>=1.36
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
matplotlib>=3.8
seaborn>=0.13
joblib>=1.3
xgboost>=2.0